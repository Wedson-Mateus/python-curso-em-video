def aumentar(n, p):
    porcentagem = p / 100
    mais = n + (n * porcentagem)
    return mais


def diminuir(n, p):
    porcentagem = p / 100
    menos = n - (n * porcentagem)
    return menos


def dobro(n):
    dob = n * 2
    return dob


def metade(n):
    met = n / 2
    return met
